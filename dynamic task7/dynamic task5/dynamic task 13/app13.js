let workspaceRegistry = [
    "Setup Environment",
    "Completed JavaScript Basics",
    "Write Functions",
    "Login Screen",
    "Dashboard UI"
];

workspaceRegistry.forEach((task, index) => {
    console.log(`${index + 1}. ${task}`);
});
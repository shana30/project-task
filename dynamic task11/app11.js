let workspaceRegistry = [
    "Setup Environment",
    "Completed JavaScript Basics",
    "Write Functions",
    "Login Screen",
    "Dashboard UI"
];

for (let i = workspaceRegistry.length - 1; i >= 0; i--) {
    console.log("Reverse Priority:", workspaceRegistry[i]);
}